# Optional: path to Google Cloud service account JSON key file.
# When set, scorecard/stats images use Google Cloud Vision for OCR (better quality).
# Get a key: Google Cloud Console → APIs & Services → Credentials → Create service account → JSON key.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account.json
